#!/usr/bin/env bash
set -euo pipefail
DIR="$( cd "$( dirname "${0}" )" >/dev/null 2>&1 && pwd )"
TAG="${TAG:-latest}"
REGISTRY="deenadayalan27"
NAME="$(basename $(dirname ${DIR}))"

echo "${REGISTRY}/${NAME}:${TAG}"
